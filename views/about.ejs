<%- include('partials/header', { title: 'À Propos' }) %>

    <!-- HEADER PAGE -->
    <header class="bg-primary pt-32 pb-20 text-center text-white relative overflow-hidden">
        <div class="absolute inset-0 opacity-20 bg-[url('https://images.unsplash.com/photo-1511649475669-e288648b2339?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80')] bg-cover bg-center"></div>
        <div class="relative z-10 max-w-4xl mx-auto px-4">
            <h1 class="text-4xl md:text-5xl font-bold mb-4">Notre Histoire & Vision</h1>
            <p class="text-xl text-slate-300">Une communauté authentique au cœur de Québec.</p>
        </div>
    </header>

    <!-- HISTOIRE -->
    <section id="vision" class="py-16 bg-white">
        <div class="max-w-4xl mx-auto px-4">
            <h2 class="text-3xl font-bold text-primary mb-6">Notre Parcours</h2>
            <p class="text-lg text-slate-600 mb-4 leading-relaxed">
                Fondée en 2015 par un petit groupe de familles désireuses de vivre leur foi de manière authentique, l'Eglise Evangelique Du Berger a commencé ses rencontres dans un salon du quartier Limoilou.
            </p>
            <p class="text-lg text-slate-600 mb-4 leading-relaxed">
                Aujourd'hui, nous sommes une communauté grandissante, reflétant la diversité de la ville de Québec. Notre rêve a toujours été de créer un espace où chacun, peu importe son arrière-plan, peut découvrir l'amour de Dieu sans jugement.
            </p>
        </div>
    </section>

    <!-- NOS VALEURS (GRID) -->
    <section id="values" class="py-16 bg-light">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-primary mb-4">Ce qui nous anime</h2>
                
                <% if (typeof user !== 'undefined' && user) { %>
                    <a href="/values/new" class="inline-flex items-center gap-2 bg-white text-primary px-5 py-2.5 rounded-full font-bold shadow-lg hover:bg-slate-100 transition transform hover:-translate-y-1 text-sm border border-slate-200">
                        <i class="ph-bold ph-plus-circle text-lg"></i> Ajouter une valeur
                    </a>
                <% } %>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <% if (typeof churchValues !== 'undefined' && churchValues.length > 0) { %>
                    <% churchValues.forEach(function(value) { %>
                    <div class="bg-white p-8 rounded-xl shadow-sm border border-slate-100 relative group hover:shadow-lg transition">
                        
                        <!-- Actions Admin (Overlay) -->
                        <% if (typeof user !== 'undefined' && user) { %>
                            <div class="absolute top-4 right-4 z-20 flex gap-2 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                                <a href="/values/edit/<%= value.id %>" class="bg-slate-100 hover:bg-blue-50 text-blue-600 p-2 rounded-full transition" title="Modifier">
                                    <i class="ph-bold ph-pencil-simple"></i>
                                </a>
                                <button type="button" onclick="openDeleteModal('value-<%= value.id %>')" class="bg-slate-100 hover:bg-red-50 text-red-600 p-2 rounded-full transition" title="Supprimer">
                                    <i class="ph-bold ph-trash"></i>
                                </button>
                                <form id="delete-form-value-<%= value.id %>" action="/values/delete/<%= value.id %>" method="POST" class="hidden"></form>
                            </div>
                        <% } %>

                        <div class="text-accent text-4xl mb-4"><i class="ph-fill <%= value.icon %>"></i></div>
                        <h3 class="text-xl font-bold mb-2"><%= value.title %></h3>
                        <p class="text-slate-600"><%= value.description %></p>
                    </div>
                    <% }); %>
                <% } else { %>
                    <p class="text-center col-span-3 text-slate-400">Aucune valeur définie pour le moment.</p>
                <% } %>
            </div>
        </div>
    </section>

    <!-- ÉQUIPE -->
    <section id="team" class="py-16 bg-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-primary mb-4">Notre Équipe</h2>
                
                <% if (typeof user !== 'undefined' && user) { %>
                    <a href="/team/new" class="inline-flex items-center gap-2 bg-primary text-white px-5 py-2.5 rounded-full font-bold shadow-lg hover:bg-slate-800 transition transform hover:-translate-y-1 text-sm">
                        <i class="ph-bold ph-plus-circle text-lg"></i> Ajouter un membre
                    </a>
                <% } %>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-10">
                <% if (team && team.length > 0) { %>
                    <% team.forEach(function(member) { %>
                    <div class="group relative bg-slate-50 rounded-2xl p-8 hover:shadow-xl transition-all duration-300 border border-slate-100 text-center">
                        
                        <!-- Actions Admin (Overlay) -->
                        <% if (typeof user !== 'undefined' && user) { %>
                            <div class="absolute top-4 right-4 z-20 flex gap-2 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                                <a href="/team/edit/<%= member.id %>" class="bg-white/90 backdrop-blur-md hover:bg-blue-50 text-blue-600 p-2.5 rounded-full shadow-lg transition-transform hover:scale-110" title="Modifier">
                                    <i class="ph-bold ph-pencil-simple"></i>
                                </a>
                                <button type="button" onclick="openDeleteModal('<%= member.id %>')" class="bg-white/90 backdrop-blur-md hover:bg-red-50 text-red-600 p-2.5 rounded-full shadow-lg transition-transform hover:scale-110" title="Supprimer">
                                    <i class="ph-bold ph-trash"></i>
                                </button>
                                <form id="delete-form-<%= member.id %>" action="/team/delete/<%= member.id %>" method="POST" class="hidden"></form>
                            </div>
                        <% } %>

                        <div class="relative w-40 h-40 mx-auto mb-6">
                             <div class="absolute inset-0 bg-primary/10 rounded-full transform -rotate-6 group-hover:rotate-6 transition-transform duration-500"></div>
                             <img src="<%= member.image_url || 'https://via.placeholder.com/400' %>" alt="<%= member.name %>" class="w-full h-full rounded-full object-cover shadow-lg relative z-10 border-4 border-white">
                        </div>
                        
                        <h3 class="text-xl font-bold text-slate-800 mb-1"><%= member.name %></h3>
                        <p class="text-accent font-bold text-xs uppercase tracking-widest mb-4"><%= member.role %></p>
                        <p class="text-slate-500 text-sm leading-relaxed"><%= member.bio %></p>
                    </div>
                    <% }); %>
                <% } else { %>
                    <p class="text-center col-span-full py-10 text-slate-400">Aucun membre d'équipe trouvé.</p>
                <% } %>
            </div>
        </div>
    </section>

    <!-- Modal de confirmation de suppression -->
    <div id="deleteModal" class="fixed inset-0 z-50 hidden items-center justify-center bg-slate-900/60 backdrop-blur-sm transition-opacity opacity-0">
        <div class="bg-white rounded-2xl shadow-2xl p-8 max-w-sm w-full mx-4 transform scale-95 transition-transform duration-200" id="deleteModalContent">
            <div class="text-center">
                <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4 text-red-600">
                    <i class="ph-bold ph-trash text-3xl"></i>
                </div>
                <h3 class="text-xl font-bold text-slate-900 mb-2">Confirmer la suppression</h3>
                <p class="text-slate-500 mb-6">Êtes-vous sûr de vouloir supprimer cet élément ? Cette action est irréversible.</p>
                <div class="flex gap-3 justify-center">
                    <button onclick="closeDeleteModal()" class="px-5 py-2.5 rounded-xl border border-slate-200 text-slate-700 font-semibold hover:bg-slate-50 transition">Annuler</button>
                    <button onclick="confirmDelete()" class="px-5 py-2.5 rounded-xl bg-red-600 text-white font-semibold hover:bg-red-700 shadow-lg shadow-red-200 transition">Supprimer</button>
                </div>
            </div>
        </div>
    </div>

    <script>
        let deleteId = null;
        const modal = document.getElementById('deleteModal');
        const modalContent = document.getElementById('deleteModalContent');

        function openDeleteModal(id) {
            deleteId = id;
            modal.classList.remove('hidden');
            modal.classList.add('flex');
            // Petit délai pour l'animation
            setTimeout(() => {
                modal.classList.remove('opacity-0');
                modalContent.classList.remove('scale-95');
                modalContent.classList.add('scale-100');
            }, 10);
        }

        function closeDeleteModal() {
            modal.classList.add('opacity-0');
            modalContent.classList.remove('scale-100');
            modalContent.classList.add('scale-95');
            setTimeout(() => {
                modal.classList.add('hidden');
                modal.classList.remove('flex');
                deleteId = null;
            }, 300);
        }

        function confirmDelete() {
            if (deleteId) {
                document.getElementById('delete-form-' + deleteId).submit();
            }
        }

        // Fermer si on clique en dehors
        modal.addEventListener('click', (e) => {
            if (e.target === modal) {
                closeDeleteModal();
            }
        });
    </script>
    
  <%- include('partials/footer') %>
